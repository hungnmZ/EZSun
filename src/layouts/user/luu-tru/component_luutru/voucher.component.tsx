import React from 'react';
import { Text } from '@ui-kitten/components'

export const VoucherScreen = ({ navigation }): React.ReactElement => {

    return (
        <Text>voucher ở đây</Text>
    );
};
