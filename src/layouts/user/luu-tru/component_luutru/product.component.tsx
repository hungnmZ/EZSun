import React from 'react';
import { Text } from '@ui-kitten/components'

export const ProductScreen = ({ navigation }): React.ReactElement => {

    return (
        <Text>sản phẩm lưu trữ ở đây</Text>
    );
};
